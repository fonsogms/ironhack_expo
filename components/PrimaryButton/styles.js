import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  buttonStyles: {
    backgroundColor: "blue",
    width: 100,
    height: 50,
    alignSelf: "center",
    justifyContent: "center",
  },
  buttonTextStyles: { color: "white", textAlign: "center" },
});
